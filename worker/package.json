{
  "name": "ledger-sync-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test password.test.js"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
